create or replace TABLE BIFROSTTEST.PUBLIC.DEMOUSERS (
	FIRSTNAME VARCHAR(100) NOT NULL,
	LASTNAME VARCHAR(79) NOT NULL,
	PHONENUMBER NUMBER(38,0),
	EMAIL VARCHAR(100) NOT NULL,
	REGISTRATIONDATE DATE NOT NULL
);
